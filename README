Installation:

	git clone https://github.com/AaronFlower/dotvim.git .vim

Create syslinks:

	ln -s ~/.vim/vimrc ~/.vimrc

Switch to the `~./vim` directory, and fetch submodules:

	cd ~/.vim
	git submodule update --init --recursive

For YouCompleteMe Plugin Your should execute:
	cd ~/.vim/bundle/YouCompleteMe
	./install.py --go-completer --js-completer --clang-completer 

Install plugins as submodudles

	mkdir ~/.vim/bundle
	git submodule add http://github.com/tpope/vim-fugitive.git bundle/fugitive
	git add .
	git commit -m "Install Fugitive.vim bundle as a submodule."

Ref: http://vimcasts.org/episodes/synchronizing-plugins-with-git-submodules-and-pathogen/


### Plugins

- [Emmet 中文参考文档](https://zfanw.com/blog/zencoding-vim-tutorial-chinese.html)

## Todo
- [ ] Use vim-plug to subsitute pathogen.
